document.addEventListener('DOMContentLoaded', function () {
    // Inicialización básica
    const mapDiv = document.getElementById('map');
    mapDiv.innerHTML = '<p>Mapa o contenido aquí...</p>';
});